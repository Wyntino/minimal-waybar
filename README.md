# minimal-waybar
waybar config that is simple, made for niri and arch but should work with most compositors and distros.

# Installation
1. Clone the repo
2. replace `~/.config/waybar` on your system with the `waybar` folder from this repository
3. optionally delete the cloned repo

this is my first ever public project, so please let me know of any oddities or bugs.
